#include <iostream>
#include <map>

using namespace std;

int main() {
    int n;
    cin >> n;
    map<string, int> n_country;
    for (int i = 0; i < n; i++){
        string country;
        cin >> country;
        string Gio_loved;
        getline(cin, Gio_loved);
    }
    system("pause");
    return 0;
}